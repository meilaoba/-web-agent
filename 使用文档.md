# AI驱动的Web代码安全审计多Agent系统 —— 使用文档

> 本文档面向**使用与部署**本系统的开发者/运维人员，涵盖：技术栈、框架结构、配置、
> 启动方式、完整接口说明、核心函数（含参数含义）与测试。
> 如需了解项目整体设计思路，请参阅同目录下《项目阅读文档.md》。
> 最后更新：2026-08-21（v0.2.0 交付版）

---

## 1. 项目简介

基于 **LLM + Multi-Agent + RAG + 静态代码分析** 的智能 Web 代码安全审计系统。

核心流程：`代码上传 → 项目解析 → 静态扫描(Bandit/Semgrep/AST) → Multi-Agent 协同审计
→ RAG 安全知识检索 → 漏洞综合判断 → 风险评级 → 修复建议 → 安全报告 → 前端展示`

---

## 2. 技术栈

### 2.1 后端（Python 3.10）
| 类别 | 技术 | 说明 |
|---|---|---|
| Web 框架 | FastAPI + Uvicorn | REST API，自带 Swagger 文档（/docs） |
| ORM / 数据库 | SQLAlchemy 2.0；SQLite（默认）/ MySQL 8.0（Docker） | 双兼容，`.env` 切换 |
| 数据校验 | Pydantic 2.x | 请求/响应模型 |
| 认证 | PyJWT（HS256）+ bcrypt | JWT + 密码哈希，登录失败锁定 |
| 静态扫描 | Bandit、Semgrep（可选）、标准库 ast | 漏洞初筛 |
| 向量库 | ChromaDB 0.5.23 | 知识检索 |
| Embedding | BGE-M3 / Hashing（降级） | 文本向量化 |
| LLM | httpx 调 OpenAI 兼容接口 | DeepSeek/Qwen/Ollama/OpenAI，可替换 |
| 文档解析 | pypdf、beautifulsoup4 | 知识文档加载 |
| 配置 | python-dotenv + dataclass | .env 配置 |
| 测试 | pytest（144 用例） | 单元/集成 |

### 2.2 前端（Node 20）
| 类别 | 技术 |
|---|---|
| 框架 | Vue 3.5（Composition API） |
| 构建 | Vite 6 |
| UI | Element Plus 2.9（中文 locale） |
| 路由 | Vue Router 4（history 模式） |
| HTTP | Axios（自动携带 JWT）+ 原生 fetch（SSE） |

### 2.3 部署
Docker Compose：`mysql:8.0` + backend（FastAPI）+ frontend（Nginx），
含健康检查；GitHub Actions CI（后端 pytest + 前端构建）。

---

## 3. 框架结构

### 3.1 后端分层
```
app/
├── config.py          # 全局配置（.env + 环境变量）
├── main.py            # FastAPI 入口（CORS / 路由注册 / 启动检查）
├── api/               # 路由层：auth / projects / audit / vulnerabilities / rag / agents / reports
├── schemas/           # Pydantic 请求/响应模型
├── models/            # SQLAlchemy ORM（10 张表）
├── services/          # 业务层：audit_service / auth_service / chat_service / db
├── agents/            # Multi-Agent：orchestrator / audit / knowledge / repair / report / llm / base / agent_log
├── rag/               # RAG：loader / cleaner / splitter / metadata / embedding / vector_store / retriever / reranker / query_classifier
└── security/          # 安全分析：project_parser / scanner / ast_analyzer / detector / models
```

### 3.2 请求处理链路
```
HTTP 请求 → api/路由（依赖注入 get_current_user 鉴权、get_db 会话）
        → services/业务服务 → models/ORM 或 agents/RAG/security 模块 → 响应 Schema
```

### 3.3 目录树（关键部分）
```
backend/
├── app/                      # 后端应用
├── data/
│   ├── raw/                  # 知识库原始数据（OWASP/CWE/CNNVD）
│   ├── processed/            # 知识处理产物（chunks.jsonl）
│   ├── uploads/              # 用户上传的项目
│   ├── agent_logs/           # Agent 执行日志 JSONL
│   └── app.db                # SQLite 数据库（默认）
├── tests/                    # pytest 测试
└── Dockerfile
frontend/
├── src/
│   ├── api/index.js          # Axios 封装 + 全部接口 + SSE
│   ├── router/index.js       # 路由 + 登录守卫
│   ├── layout/MainLayout.vue # 主布局
│   └── views/                # 页面
├── package.json / vite.config.js
└── Dockerfile + nginx.conf
scripts/
├── data_pipeline/            # 知识库构建（build/ingest/init）
├── retrieval/                # 检索演示与评估
├── launch/                   # 启动脚本
└── dev_tools/                # 冒烟测试等
docker-compose.yml            # MySQL + 后端 + 前端
.github/workflows/ci.yml      # CI
```

---

## 4. 环境准备与快速启动

### 4.1 环境要求
- Python 3.10
- Node.js 20（前端）
- MySQL 8.0（可选，本地默认 SQLite 无需）
- Docker（可选，容器部署）

### 4.2 配置
1. 复制 `.env.example` 为 `.env`：`Copy-Item .env.example .env`（Windows）或 `cp .env.example .env`；
2. 按需修改：LLM Provider / Key、数据库、JWT_SECRET 等（见第 5 节）。

### 4.3 本地启动（推荐一键脚本）
```bash
scripts\launch\start_all.cmd
# 自动：检查依赖 → 安装前端依赖 → 初始化知识库 → 启动后端(8000) + 前端(5173)
```

手动启动：
```bash
# 后端
pip install -r backend/requirements.txt
python scripts/data_pipeline/init_knowledge.cmd   # 可选，初始化知识库（启用 RAG）
python scripts/launch/run_backend.cmd             # 或: cd backend && python -m uvicorn app.main:app --port 8000

# 前端
cd frontend && npm install && npm run dev         # http://127.0.0.1:5173
```

### 4.4 Docker 部署
```bash
docker compose up -d --build
# 前端 http://localhost ，API 文档 http://localhost:8000/docs
```

### 4.5 测试
```bash
cd backend && python -m pytest tests -v     # 全量 144 用例
python scripts/dev_tools/smoke_test.py      # 对运行中的后端做冒烟验证
```

---

## 5. 配置说明（.env）

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `LLM_PROVIDER` | `openai_compatible` | `qwen` / `deepseek` / `openai` / `ollama` / `mock` 等 |
| `LLM_API_KEY` | 空 | 未配置时自动使用 MockLLM（离线演示） |
| `LLM_BASE_URL` | 按 Provider | 可覆盖（如 Ollama `http://localhost:11434/v1`） |
| `LLM_MODEL` | `deepseek-chat` | 模型名（qwen-max / gpt-4o-mini 等） |
| `LLM_TEMPERATURE` | 0.2 | LLM 采样温度 |
| `EMBEDDING_PROVIDER` | `hashing` | `bge_m3`（本地模型）/ `hashing`（降级） |
| `EMBEDDING_MODEL` / `EMBEDDING_DEVICE` | `BAAI/bge-m3` / `cpu` | BGE-M3 模型与设备 |
| `CHROMA_DIR` | 自动选择 | **必须为纯 ASCII 路径**（中文路径会导致 HNSW 索引失败） |
| `CHROMA_COLLECTION` | `security_knowledge` | 向量集合名 |
| `RETRIEVAL_TOP_K` | 5 | 默认检索条数 |
| `RERANK_ENABLED` | `true` | 是否启用重排 |
| `CHUNK_SIZE` / `CHUNK_OVERLAP` | 800 / 100 | 知识分割参数 |
| `DB_TYPE` | `sqlite` | `sqlite` / `mysql` |
| `DB_HOST/PORT/USER/PASSWORD/NAME` | - | MySQL 连接参数 |
| `JWT_SECRET` | dev 默认 | **生产务必设置强随机密钥** |
| `TOKEN_EXPIRE_HOURS` | 168 | Token 有效期（小时） |
| `LOGIN_MAX_ATTEMPTS` / `LOGIN_LOCK_MINUTES` | 5 / 15 | 防暴力破解 |
| `CORS_ORIGINS` | 空（允许所有） | 逗号分隔的前端域名白名单（生产建议配置） |
| `API_HOST` / `API_PORT` | 127.0.0.1 / 8000 | 服务监听地址 |
| `LOG_LEVEL` | INFO | 日志级别 |

> ⚠️ 安全注意：`.env` 包含密钥，已被 `.gitignore` 忽略，切勿提交；`.env.example` 仅放占位符。
> 生产环境请设置强随机 JWT_SECRET（`python -c "import secrets; print(secrets.token_hex(32))"`）。

---

## 6. REST API 接口文档

Base URL：`http://127.0.0.1:8000`；除注册/登录外均需请求头 `Authorization: Bearer <token>`。

### 6.1 系统
| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| GET | `/` | 否 | 服务信息 |
| GET | `/api/health` | 否 | 健康检查 |

### 6.2 认证 `/api/auth`
| 方法 | 路径 | 请求体 | 响应 | 说明 |
|---|---|---|---|---|
| POST | `/api/auth/register` | `{username(3-64), password(6-128)}` | `TokenResponse` | 注册并签发 Token |
| POST | `/api/auth/login` | `{username, password}` | `TokenResponse` | 登录；连续失败锁定 |

`TokenResponse = {access_token, token_type, user:{id, username, role, created_at}}`

### 6.3 项目 `/api/projects`
| 方法 | 路径 | 请求体/参数 | 响应 | 说明 |
|---|---|---|---|---|
| GET | `/api/projects` | - | `ProjectResponse[]` | 项目列表 |
| POST | `/api/projects` | `{name(1-128), description?}` | `ProjectResponse`(201) | 创建项目 |
| GET | `/api/projects/{id}` | - | `ProjectResponse` | 项目详情 |
| POST | `/api/projects/{id}/upload` | `multipart: file(zip)` | `UploadResponse` | 上传 zip（仅 .zip） |
| DELETE | `/api/projects/{id}` | - | 204 | 删除项目 |

`ProjectResponse = {id, name, language, description, file_count, created_at}`
`UploadResponse = {project_id, name, file_count, language, message}`

### 6.4 审计 `/api/audit`
| 方法 | 路径 | 请求体/参数 | 响应 | 说明 |
|---|---|---|---|---|
| POST | `/api/audit/tasks` | `{project_id, enable_knowledge?=true}` | `AuditStatusResponse`(201) | 创建并同步执行审计 |
| GET | `/api/audit/tasks` | `project_id`（必填），`limit=100(1-500)`，`offset=0` | `AuditStatusResponse[]` | 任务列表（分页） |
| GET | `/api/audit/tasks/{id}` | - | `AuditStatusResponse` | 任务状态 |
| GET | `/api/audit/tasks/{id}/result` | - | `AuditResultResponse` | 完整结果（报告+Agent 链） |

`AuditStatusResponse = {id, task_id, status, language, scanned_files, total_findings, security_score, started_at, finished_at}`
`AuditResultResponse = {task_id, status, duration, report, agent_chain[]}`

### 6.5 漏洞与修复 `/api/vulnerabilities`
| 方法 | 路径 | 参数 | 响应 | 说明 |
|---|---|---|---|---|
| GET | `/api/vulnerabilities` | `task_id`（必填），`limit=200(1-1000)`，`offset=0` | `VulnerabilityResponse[]` | 漏洞列表（分页） |
| GET | `/api/vulnerabilities/{id}/suggestions` | - | `RepairSuggestionResponse[]` | 修复建议 |

`VulnerabilityResponse = {id, task_id, file_path, line, vulnerability_type, severity, cwe_id, scanner, rule_id, confirmed, evidence, reason}`
`RepairSuggestionResponse = {id, vulnerability_id, root_cause, principle, suggestion, fixed_code, references[], apply_to_source}`

### 6.6 Agent 过程 `/api/agents`
| 方法 | 路径 | 响应 | 说明 |
|---|---|---|---|
| GET | `/api/agents/tasks/{id}/logs` | `AgentLogResponse[]` | Agent 执行链 |

`AgentLogResponse = {id, agent_name, input_summary, output_summary, status, duration, details, start_time}`

### 6.7 报告 `/api/reports`
| 方法 | 路径 | 参数 | 响应 | 说明 |
|---|---|---|---|---|
| GET | `/api/reports/tasks/{id}` | `fmt=json`(默认)/`markdown` | JSON 或 Markdown | 审计报告 |

### 6.8 RAG `/api/rag`
| 方法 | 路径 | 请求体/参数 | 响应 | 说明 |
|---|---|---|---|---|
| POST | `/api/rag/search` | `{query, top_k?(1-20), metadata_filter?, rerank?}` | `RagChunkResponse[]` | 知识检索 |
| GET | `/api/rag/stats` | - | `{collection, chunk_count}` | 知识库统计 |
| GET | `/api/rag/sessions` | - | `ChatSessionResponse[]` | 会话列表 |
| POST | `/api/rag/sessions` | `{title?}` | `ChatSessionResponse`(201) | 新建会话 |
| GET | `/api/rag/sessions/{id}/messages` | - | `ChatMessageResponse[]` | 会话历史 |
| DELETE | `/api/rag/sessions/{id}` | - | 204 | 删除会话 |
| POST | `/api/rag/chat` | `{message(1-4000), session_id?, top_k?}` | SSE 流式 | 智能对话 |

**`/api/rag/chat` SSE 事件**：
```
event: status   data: {"message": "正在检索知识库..."}
event: sources  data: [{"title","document","category","score","preview"}]
event: token    data: "增量文本"
event: meta     data: {"used_rag": true}
event: done     data: {"session_id": 1}
event: error    data: {"message": "..."}
```

---

## 7. 核心函数说明（含参数含义）

> 路径均相对 `backend/`。`*` 表示关键字参数（必须按名传）。

### 7.1 业务服务 `app/services/audit_service.py`

| 函数 | 参数 | 返回 | 说明 |
|---|---|---|---|
| `AuditService.create_project` | `user_id:int` 用户ID；`name:str` 项目名；`description:str=""` 描述 | `Project` | 创建项目 |
| `AuditService.get_project` | `project_id:int`；`user_id:int` | `Project\|None` | 按归属查询项目 |
| `AuditService.list_projects` | `user_id:int` | `list[Project]` | 用户项目列表 |
| `AuditService.delete_project` | `project:Project` | `None` | 删除项目（含磁盘目录） |
| `AuditService.upload_zip` | `user_id:int`；`project:Project`；`zip_path:Path` | `dict{file_count,language}` | 安全解压 zip 并登记文件 |
| `AuditService.run_audit` | `project:Project`；`enable_knowledge:bool=True` 是否启用 RAG 知识 | `AuditTask` | 执行完整审计并落库 |
| `AuditService.task_belongs_to_user` | `task` 任务对象；`user_id:int` | `bool` | 校验任务归属（新增） |
| `AuditService.get_task` | `task_id:int` | `AuditTask\|None` | 查询任务 |
| `AuditService.list_tasks` | `project_id:int`；`limit:int=100` 条数上限；`offset:int=0` 偏移 | `list[AuditTask]` | 任务列表（分页） |
| `AuditService.get_vulnerabilities` | `task_id:int`；`limit:int=200`；`offset:int=0` | `list[Vulnerability]` | 漏洞列表（分页） |
| `AuditService.get_suggestions` | `vulnerability_id:int` | `list[RepairSuggestion]` | 修复建议 |
| `AuditService.get_agent_logs` | `task_id:int` | `list[AgentLog]` | Agent 执行链 |
| `AuditService.get_report` | `task_id:int` | `AuditReport\|None` | 报告快照 |

### 7.2 对话服务 `app/services/chat_service.py`

| 函数 | 参数 | 返回 | 说明 |
|---|---|---|---|
| `ChatService.create_session` | `user_id:int`；`title:str="新会话"` | `ChatSession` | 新建会话 |
| `ChatService.list_sessions` | `user_id:int` | `list[ChatSession]` | 会话列表 |
| `ChatService.get_session` | `session_id:int`；`user_id:int` | `ChatSession\|None` | 查会话（归属校验） |
| `ChatService.delete_session` | `session:ChatSession` | `None` | 删除会话及消息 |
| `ChatService.get_messages` | `session_id:int`；`limit:int=200` | `list[ChatMessage]` | 会话历史 |
| `ChatService.chat_stream` | `user_id:int`；`message:str` 用户问题；`session_id:int\|None` 会话ID；`top_k:int=5` 检索条数 | `Iterator[dict]` | 流式对话事件（status/sources/token/done/error） |

### 7.3 认证服务 `app/services/auth_service.py`

| 函数 | 参数 | 返回 | 说明 |
|---|---|---|---|
| `hash_password` | `password:str` | `str` | bcrypt 加盐哈希 |
| `verify_password` | `password:str`；`password_hash:str` | `bool` | 校验密码 |
| `create_token` | `user_id:int`；`username:str`；`expires_hours:int=168` | `str` | 签发 JWT |
| `decode_token` | `token:str` | `dict\|None` | 解析 JWT（失败返回 None） |

### 7.4 Multi-Agent `app/agents/`

| 函数 | 参数 | 返回 | 说明 |
|---|---|---|---|
| `Orchestrator.run_audit` | `scan_result:dict` 扫描结果；`project:dict\|None` 项目信息；`task_id:str\|None`；`enable_knowledge:bool=True` | `dict{task_id,status,duration,report,agent_chain}` | 编排完整审计 |
| `BaseAgent.run` | `context:AgentContext`；`**kwargs` | `dict{agent,task_id,status,duration,output}` | 执行并记录日志 |
| `AgentContext.get/set/update` | key/value | 任意 | 任务上下文读写 |
| `LLMClient.chat` | `system_prompt:str`；`user_prompt:str`；`temperature:float\|None`；`max_tokens:int=2048` | `str` | 一次性对话 |
| `LLMClient.chat_stream` | `messages:list[dict]` OpenAI 消息；`temperature`；`max_tokens` | `Iterator[str]` | 流式对话 |
| `get_llm_client` | 无 | `LLMClient` | 工厂（按配置，进程内缓存） |
| `AgentLogger.start_task` | `task_id:str\|None` | `str` | 开启日志任务 |
| `AgentLogger.log` | `task_id`；`agent_name`；`input_summary`；`output_summary=""`；`status="completed"`；`duration=0.0`；`details=None` | `None` | 记录一条执行日志（按任务写独立文件） |
| `AgentLogger.get_task_logs` | `task_id:str` | `list[dict]` | 读取执行链 |

### 7.5 RAG `app/rag/`

| 函数 | 参数 | 返回 | 说明 |
|---|---|---|---|
| `Retriever.retrieve` | `query:str` 问题；`top_k:int\|None` 返回条数；`metadata_filter:dict\|None` 元数据过滤；`where_document:dict\|None` 正文过滤；`rerank:bool\|None` 是否重排；`initial_pool:int=20` 候选池；`keyword_fallback:bool=True` 关键词兜底 | `list[RetrievedChunk]` | 核心检索入口 |
| `Retriever.search_by_cwe` | `cwe_id:str` 如 CWE-89；`top_k:int=5` | `list[RetrievedChunk]` | 按 CWE 检索 |
| `Retriever.search_by_vulnerability` | `vuln_type:str`；`top_k:int=5` | `list[RetrievedChunk]` | 按漏洞类型检索 |
| `get_default_retriever` | `top_k:int\|None` | `Retriever` | 工厂（进程内缓存） |
| `extract_keywords` | `query:str` | `list[str]` | 关键词提取（安全术语扩展） |
| `ChromaVectorStore.add_documents` | `documents:list[Document]`；`embeddings:list[list[float]]` | `int` | 批量入库（按 chunk_id 幂等） |
| `SemanticRecursiveTextSplitter` | `chunk_size:int=800`；`chunk_overlap:int=100`；`separators`；`tokenizer`；`min_chunk_size:int=50` | - | 语义感知分割器 |
| `.split_documents` | `docs:list[Document]` | `list[Document]` | 执行分割 |
| `estimate_tokens` | `text:str` | `int` | 近似 token 估算 |
| `TextCleaner.clean` | `text:str` | `str` | 清洗单段文本（保护代码块） |
| `TextCleaner.clean_documents` | `docs:list[Document]` | `list[Document]` | 批量清洗 |
| `extract_knowledge_metadata` | `text:str` | `dict{cwe_id,cve_id,owasp_id}` | 提取知识编号 |
| `normalize_metadata` | `metadata:dict` | `dict` | 规范化为 ChromaDB 类型 |
| `classify_security_query` | `query:str`；`last_user_message:str\|None` | `bool` | 是否安全问题 |

### 7.6 安全分析 `app/security/`

| 函数 | 参数 | 返回 | 说明 |
|---|---|---|---|
| `ProjectParser.extract_zip` | `zip_path:Path`；`target_name:str` | `Path` | 安全解压（防 Zip Slip） |
| `ProjectParser.collect_source_files` | `project_dir:Path` | `list[Path]` | 收集代码文件（跳过依赖目录） |
| `ProjectParser.detect_language` | `files:list[Path]` | `str` | 语言识别 |
| `ProjectParser.read_file` | `path:Path`；`max_bytes:int=200000` | `str` | 读取文件（多编码） |
| `VulnerabilityDetector.scan_project` | `project_dir`；`project_name:str`；`parser=None` | `ScanResult` | 组合扫描 + 去重 |
| `BanditScanner.scan` / `SemgrepScanner.scan` | `files:list[Path]`；`project_root:Path` | `list[ScanFinding]` | 各扫描器执行 |
| `AstAnalyzer.scan` | `files:list[Path]`；`project_root:Path` | `list[ScanFinding]` | AST 危险模式分析 |

---

## 8. 测试与 CI

- **本地测试**：`cd backend && python -m pytest tests -v`（144 用例，覆盖 API/Agent/RAG/安全分析）；
- **冒烟测试**：`python scripts/dev_tools/smoke_test.py`（需后端已启动）；
- **CI**：`.github/workflows/ci.yml` 在 push/PR 时自动执行后端 pytest 与前端构建。

---

## 9. 常见问题与安全注意事项

| 问题 | 处理 |
|---|---|
| 未配置 LLM_API_KEY 能否运行？ | 可以，自动使用 MockLLM，可离线演示完整流程 |
| 中文路径导致 ChromaDB 报错？ | 设置 `CHROMA_DIR` 为纯 ASCII 路径（如 `C:\chroma_data`） |
| 生产部署 JWT 风险 | 必须设置强随机 `JWT_SECRET`，后端在 MySQL 模式下会告警 |
| 跨域被拦截 | 生产设置 `CORS_ORIGINS` 为前端域名 |
| 知识库为空导致检索无结果 | 执行 `python scripts/data_pipeline/init_knowledge.cmd` 初始化知识库 |
| `.env` 泄露风险 | `.env` 已被 gitignore；`.env.example` 仅占位符，切勿写入真实密钥 |
